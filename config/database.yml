development:
  adapter: sqlite3
  database: pikifriends-development
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: pikifriends-test
  host: localhost

production:
  adapter: sqlite3
  database: pikifriends-production
  host: localhost